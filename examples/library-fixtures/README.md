# Workspace Library fixtures

`deterministic.docx` is a deliberately small Word document used to pin Mammoth's
Markdown output, derived-reader path, source hash, and deterministic chunk locator.
It contains a heading, two paragraphs, non-ASCII text, and a surrogate pair.

// GENERATED FILE — DO NOT EDIT BY HAND.
//
// Produced by scripts/generate-memory-index-cli.mjs (esbuild bundle of
// scripts/memory-index-cli-entry.ts + src/shared/memory-index-core.ts).
// Regenerate with `node scripts/generate-memory-index-cli.mjs` (runs inside
// `npm run build:main`). The drift test guards byte-identity.

export const MEMORY_INDEX_MJS: string = "// scripts/memory-index-cli-entry.ts\nimport * as fs2 from \"fs\";\nimport * as path2 from \"path\";\n\n// src/shared/memory-index-core.ts\nvar MEMORY_INDEX_BUDGET_BYTES = 25e3;\nvar MEMORY_INDEX_BUDGET_LINES = 200;\nvar ARCHIVE_BUDGET_BYTES = 2e5;\nvar ARCHIVE_BUDGET_LINES = 2e3;\nvar CAP_PRESSURE_RATIO = 0.8;\nvar DISCLOSURE_FORMAT_MARKER = \"<!-- disclosure-format: v2 -->\";\nvar ARCHIVE_FORMAT_MARKER = \"<!-- memory-archive-format: v1 -->\";\nvar MEMORY_FRAMING_PREAMBLE = `**Your workspace memories.** Each entry below is a short description of something this workspace learned. They are deliberately brief \\u2014 the detail is not here. If at any point in your work an entry looks relevant, call \\`recall_memory\\` with its id to read the full memory. Nothing will remind you; deciding what is worth opening is yours.`;\nvar MEMORY_ID_GRAMMAR = /^mb-\\d{4}-\\d{2}-\\d{2}-[a-z0-9-]+$/;\nvar MEMORY_DETAILS_DIR = \".lares/supervisor/memory/details/\";\nvar MEMORY_ARCHIVE_DIR = \".lares/supervisor/memory/archive/\";\nvar ARCHIVE_INDEX_REL = \".lares/supervisor/memory/archive/ARCHIVE.md\";\nvar DISPOSAL_BLOCK_RE = /^(?:[ \\t]*\\n)*<!-- memory-disposal:v1\\n([\\s\\S]*?)\\n-->(?:\\n|$)/;\nvar _enc = new TextEncoder();\nvar _CAPSULE_HEADING = /^##\\s+(mb-\\S+?):\\s*(.*)$/;\nvar _FIELD_LINE = /^-\\s+([a-z][a-z-]*):\\s*(.*)$/;\nvar _SCOPED_PKG_RE = /@[a-z0-9][a-z0-9-]*\\/[a-z0-9][a-z0-9._-]*/g;\nfunction utf8ByteLength(str) {\n  return _enc.encode(str).length;\n}\nfunction countLines(text) {\n  const normalized = text.replace(/\\r\\n/g, \"\\n\");\n  let count = 1;\n  for (let i = 0; i < normalized.length; i++) if (normalized.charCodeAt(i) === 10) count++;\n  return count;\n}\nfunction isValidMemoryId(id) {\n  return MEMORY_ID_GRAMMAR.test(id);\n}\nfunction detectBareScopedPkg(text) {\n  const noFenced = text.replace(/```[\\s\\S]*?```/g, \" \");\n  const noInline = noFenced.replace(/`[^`\\n]*`/g, \" \");\n  const out = [];\n  let match;\n  _SCOPED_PKG_RE.lastIndex = 0;\n  while ((match = _SCOPED_PKG_RE.exec(noInline)) !== null) out.push(match[0]);\n  return out;\n}\nfunction realGregorianDate(value) {\n  const match = /^(\\d{4})-(\\d{2})-(\\d{2})$/.exec(value);\n  if (!match) return false;\n  const year = Number(match[1]);\n  const month = Number(match[2]);\n  const day = Number(match[3]);\n  const date = new Date(Date.UTC(year, month - 1, day));\n  return date.toISOString().slice(0, 10) === value;\n}\nfunction parseDisposal(bodyText) {\n  const normalized = bodyText.replace(/^\\uFEFF/, \"\").replace(/\\r\\n/g, \"\\n\");\n  const markerCount = (normalized.match(/^[ \\t]*<!-- memory-disposal:v1[ \\t]*$/gm) ?? []).length;\n  if (markerCount === 0) {\n    return normalized.includes(\"<!-- memory-disposal:v1\") ? { ok: false, error: \"malformed\", message: \"memory-disposal:v1 opener must be exact\" } : { ok: false, error: \"missing\", message: \"missing leading memory-disposal:v1 block\" };\n  }\n  if (markerCount !== 1) {\n    return { ok: false, error: \"malformed\", message: \"expected exactly one memory-disposal:v1 block\" };\n  }\n  const match = DISPOSAL_BLOCK_RE.exec(normalized);\n  if (!match) {\n    return { ok: false, error: \"malformed\", message: \"memory-disposal:v1 block must be first and exact\" };\n  }\n  const fields = {};\n  for (const line of match[1].split(\"\\n\")) {\n    const field = /^([a-z][a-z-]*):[ \\t]*(.*)$/.exec(line);\n    if (!field) return { ok: false, error: \"malformed\", message: `invalid disposal content: ${line}` };\n    const key = field[1];\n    if (key !== \"kind\" && key !== \"value\") {\n      return { ok: false, error: \"malformed\", message: `unknown disposal key: ${key}` };\n    }\n    if (key in fields) return { ok: false, error: \"malformed\", message: `duplicate disposal key: ${key}` };\n    fields[key] = field[2].trim();\n  }\n  const kind = fields.kind;\n  if (kind !== \"expires\" && kind !== \"expires-when\" && kind !== \"open-loop\") {\n    return { ok: false, error: \"malformed\", message: `invalid disposal kind: ${kind ?? \"\"}` };\n  }\n  const hasValue = Object.prototype.hasOwnProperty.call(fields, \"value\");\n  if (kind === \"open-loop\") {\n    if (hasValue) return { ok: false, error: \"malformed\", message: \"open-loop forbids value\" };\n  } else if (!hasValue) {\n    return { ok: false, error: \"malformed\", message: `${kind} requires value` };\n  }\n  if (kind === \"expires\" && !realGregorianDate(fields.value)) {\n    return { ok: false, error: \"malformed\", message: \"expires value must be a real YYYY-MM-DD date\" };\n  }\n  if (kind === \"expires-when\" && !fields.value) {\n    return { ok: false, error: \"malformed\", message: \"expires-when value must be non-empty\" };\n  }\n  return {\n    ok: true,\n    disposal: {\n      kind,\n      value: kind === \"open-loop\" ? null : fields.value,\n      blockStart: match.index,\n      blockEnd: match.index + match[0].length\n    }\n  };\n}\nfunction parseHandoff(preamble) {\n  const heading = /^##\\s+handoff-read-first\\s*$/m.exec(preamble);\n  if (!heading) return { ids: null, range: null };\n  const afterHeading = heading.index + heading[0].length;\n  const nextHeading = /^##\\s+/m.exec(preamble.slice(afterHeading));\n  const end = nextHeading ? afterHeading + nextHeading.index : preamble.length;\n  const section = preamble.slice(heading.index, end);\n  const ids = [];\n  for (const line of section.split(\"\\n\").slice(1)) {\n    const item = /^\\s*\\d+\\.\\s+(\\S+)/.exec(line);\n    if (item) ids.push(item[1]);\n  }\n  return { ids, range: [heading.index, end] };\n}\nfunction parseCatalog(text, marker, markerName) {\n  const raw = text;\n  const normalized = raw.replace(/\\r\\n/g, \"\\n\");\n  const lines = normalized.split(\"\\n\");\n  let hasMarker = false;\n  let markerMismatch = false;\n  for (const line of lines) {\n    const trimmed = line.trim();\n    if (trimmed === marker) {\n      hasMarker = true;\n      break;\n    }\n    if (new RegExp(`^<!--\\\\s*${markerName}:`).test(trimmed)) markerMismatch = true;\n  }\n  const headings = [];\n  let offset = 0;\n  for (let i = 0; i < lines.length; i++) {\n    if (_CAPSULE_HEADING.test(lines[i])) headings.push({ offset, lineIdx: i });\n    offset += lines[i].length + 1;\n  }\n  const firstOffset = headings.length ? headings[0].offset : normalized.length;\n  const preamble = normalized.slice(0, firstOffset);\n  const handoff = parseHandoff(preamble);\n  const entries = [];\n  for (let h = 0; h < headings.length; h++) {\n    const blockStart = headings[h].offset;\n    const blockEnd = h + 1 < headings.length ? headings[h + 1].offset : normalized.length;\n    const blockLines = normalized.slice(blockStart, blockEnd).split(\"\\n\");\n    const heading = blockLines[0].match(_CAPSULE_HEADING);\n    const id = heading[1];\n    const fields = {};\n    const duplicateFields = [];\n    const unexpectedContent = [];\n    for (const line of blockLines.slice(1)) {\n      if (line.trim() === \"\") continue;\n      const field = line.match(_FIELD_LINE);\n      if (!field) {\n        unexpectedContent.push(line);\n        continue;\n      }\n      const key = field[1];\n      if (key in fields) {\n        if (!duplicateFields.includes(key)) duplicateFields.push(key);\n      } else {\n        fields[key] = field[2].trim();\n      }\n    }\n    entries.push({\n      id,\n      title: heading[2].trim(),\n      status: fields.status ?? \"active\",\n      idDate: /^mb-(\\d{4}-\\d{2}-\\d{2})-/.exec(id)?.[1] ?? null,\n      readIf: fields[\"read-if\"] ?? null,\n      detail: fields.detail ?? null,\n      fields,\n      duplicateFields,\n      unexpectedContent,\n      blockStart,\n      blockEnd,\n      idMalformed: !isValidMemoryId(id),\n      date: fields.date ?? null,\n      owner: fields.owner ?? null,\n      consequence: fields.consequence ?? null,\n      state: fields.state ?? null,\n      openLoop: fields[\"open-loop\"] ?? null,\n      expires: fields.expires ?? null,\n      expiresWhen: fields[\"expires-when\"] ?? null\n    });\n  }\n  return {\n    raw,\n    normalized,\n    byteLength: utf8ByteLength(raw),\n    lineCount: countLines(raw),\n    hasMarker,\n    markerMismatch,\n    preamble,\n    handoffReadFirst: handoff.ids,\n    handoffRange: handoff.range,\n    entries,\n    bareScopedPkgs: detectBareScopedPkg(normalized).map((token) => ({ token }))\n  };\n}\nfunction parseIndex(text) {\n  return parseCatalog(text, DISCLOSURE_FORMAT_MARKER, \"disclosure-format\");\n}\nfunction parseArchiveIndex(text) {\n  return parseCatalog(text, ARCHIVE_FORMAT_MARKER, \"memory-archive-format\");\n}\nfunction hard(cls, id, message, entry) {\n  return entry ? { cls, severity: \"hard\", id, message, blockStart: entry.blockStart, blockEnd: entry.blockEnd } : { cls, severity: \"hard\", id, message };\n}\nfunction advisory(cls, id, message) {\n  return { cls, severity: \"advisory\", id, message };\n}\nfunction validateEntries(parsed, archive) {\n  const findings = [];\n  const counts = /* @__PURE__ */ new Map();\n  for (const entry of parsed.entries) counts.set(entry.id, (counts.get(entry.id) ?? 0) + 1);\n  for (const entry of parsed.entries) {\n    if (entry.idMalformed) findings.push(hard(\"malformed-schema\", entry.id, `malformed memory id: ${entry.id}`, entry));\n    if ((counts.get(entry.id) ?? 0) > 1) findings.push(hard(\"duplicate-id\", entry.id, `duplicate memory id: ${entry.id}`, entry));\n    const allowed = archive ? /* @__PURE__ */ new Set([\"status\", \"detail\"]) : /* @__PURE__ */ new Set([\"read-if\", \"detail\"]);\n    for (const key of Object.keys(entry.fields)) {\n      if (!allowed.has(key)) findings.push(hard(\"unexpected-field\", entry.id, `unexpected field \"${key}\" on ${entry.id}`, entry));\n    }\n    for (const key of entry.duplicateFields) {\n      findings.push(hard(\"duplicate-field\", entry.id, `duplicate field \"${key}\" on ${entry.id}`, entry));\n    }\n    if (entry.unexpectedContent.length) {\n      findings.push(hard(\"unexpected-content\", entry.id, `unexpected content in ${entry.id}`, entry));\n    }\n    if (archive) {\n      if (entry.status !== \"archived\") findings.push(hard(\"malformed-schema\", entry.id, `archive entry ${entry.id} must have status archived`, entry));\n    } else if (entry.status !== \"active\") {\n      findings.push(hard(\"malformed-schema\", entry.id, `resident entry ${entry.id} must be active`, entry));\n    }\n    if (!entry.detail) findings.push(hard(\"missing-field\", entry.id, `missing required field \"detail\" on ${entry.id}`, entry));\n    if (!archive && !entry.readIf) findings.push(hard(\"missing-field\", entry.id, `missing required field \"read-if\" on ${entry.id}`, entry));\n  }\n  return findings;\n}\nfunction validateParsed(parsed) {\n  const hardFindings = validateEntries(parsed, false);\n  const advisoryFindings = [];\n  if (!parsed.hasMarker) {\n    hardFindings.push(hard(\"legacy-format\", null, parsed.markerMismatch ? `disclosure-format marker mismatched; expected ${DISCLOSURE_FORMAT_MARKER}` : `missing ${DISCLOSURE_FORMAT_MARKER}`));\n  }\n  if (parsed.handoffReadFirst) {\n    const ids = new Set(parsed.entries.map((entry) => entry.id));\n    if (parsed.handoffReadFirst.length > 5) hardFindings.push(hard(\"invalid-handoff\", null, \"handoff-read-first exceeds 5 entries\"));\n    for (const id of parsed.handoffReadFirst) {\n      if (!isValidMemoryId(id) || !ids.has(id)) hardFindings.push(hard(\"invalid-handoff\", id, `handoff-read-first references unknown/invalid id: ${id}`));\n    }\n  }\n  for (const { token } of parsed.bareScopedPkgs) hardFindings.push(hard(\"bare-scoped-pkg\", null, `unbackticked package token in prose: ${token}`));\n  if (parsed.byteLength > MEMORY_INDEX_BUDGET_BYTES) hardFindings.push(hard(\"byte-budget\", null, `index is ${parsed.byteLength} bytes (budget ${MEMORY_INDEX_BUDGET_BYTES})`));\n  if (parsed.lineCount > MEMORY_INDEX_BUDGET_LINES) hardFindings.push(hard(\"line-budget\", null, `index is ${parsed.lineCount} lines (budget ${MEMORY_INDEX_BUDGET_LINES})`));\n  if (parsed.byteLength / MEMORY_INDEX_BUDGET_BYTES > CAP_PRESSURE_RATIO || parsed.lineCount / MEMORY_INDEX_BUDGET_LINES > CAP_PRESSURE_RATIO) {\n    advisoryFindings.push(advisory(\"cap-pressure\", null, `index over ${Math.round(CAP_PRESSURE_RATIO * 100)}% of a budget`));\n  }\n  return { hard: hardFindings, advisory: advisoryFindings };\n}\nfunction validateArchiveParsed(parsed) {\n  const hardFindings = validateEntries(parsed, true);\n  const advisoryFindings = [];\n  if (!parsed.hasMarker) {\n    hardFindings.push(hard(\"legacy-archive-format\", null, parsed.markerMismatch ? `memory archive marker mismatched; expected ${ARCHIVE_FORMAT_MARKER}` : `missing ${ARCHIVE_FORMAT_MARKER}`));\n  }\n  if (parsed.handoffReadFirst) {\n    const [blockStart, blockEnd] = parsed.handoffRange;\n    hardFindings.push({\n      ...hard(\"unexpected-content\", null, \"archive catalog forbids handoff-read-first\"),\n      blockStart,\n      blockEnd\n    });\n  }\n  if (parsed.byteLength > ARCHIVE_BUDGET_BYTES || parsed.lineCount > ARCHIVE_BUDGET_LINES) {\n    advisoryFindings.push(advisory(\"archive-growth\", null, `archive is ${parsed.byteLength} bytes and ${parsed.lineCount} lines`));\n  }\n  return { hard: hardFindings, advisory: advisoryFindings };\n}\nvar LOCAL_SPLICE_CLASSES = /* @__PURE__ */ new Set([\n  \"malformed-schema\",\n  \"unexpected-field\",\n  \"unexpected-content\",\n  \"duplicate-field\",\n  \"disposal-missing\",\n  \"disposal-malformed\",\n  \"detail-missing\",\n  \"detail-escape\",\n  \"detail-unreadable\",\n  \"detail-root-mismatch\",\n  \"duplicate-id\",\n  \"missing-field\"\n]);\nvar GLOBAL_BLANK_CLASSES = /* @__PURE__ */ new Set([\"legacy-format\", \"bare-scoped-pkg\"]);\nfunction spliceRanges(source, ranges) {\n  if (!ranges.length) return source;\n  const sorted = [...ranges].sort((a, b) => a[0] - b[0]);\n  let cursor = 0;\n  const parts = [];\n  for (const [start, end] of sorted) {\n    if (start < cursor) continue;\n    parts.push(source.slice(cursor, start));\n    cursor = end;\n  }\n  parts.push(source.slice(cursor));\n  return parts.join(\"\");\n}\nfunction projectionBudget(text) {\n  const bytes = utf8ByteLength(text);\n  const lines = countLines(text);\n  return {\n    bytes,\n    lines,\n    byteBudget: MEMORY_INDEX_BUDGET_BYTES,\n    lineBudget: MEMORY_INDEX_BUDGET_LINES,\n    byteRatio: bytes / MEMORY_INDEX_BUDGET_BYTES,\n    lineRatio: lines / MEMORY_INDEX_BUDGET_LINES,\n    overBudget: bytes > MEMORY_INDEX_BUDGET_BYTES || lines > MEMORY_INDEX_BUDGET_LINES\n  };\n}\nfunction frameMemoryProjection(text) {\n  return `${MEMORY_FRAMING_PREAMBLE}\n\n${text}`;\n}\nfunction projectParsed(parsed, opts) {\n  void opts.nowISO;\n  const validation = validateParsed(parsed);\n  const allFindings = [...validation.hard, ...opts.entryFindings ?? []];\n  const expiredSet = new Set(opts.expiredIds ?? []);\n  const expired = [];\n  const shed = [];\n  const removedRanges = [];\n  const removedEntries = /* @__PURE__ */ new Set();\n  for (const entry of parsed.entries) {\n    if (entry.status !== \"active\") {\n      removedRanges.push([entry.blockStart, entry.blockEnd]);\n      removedEntries.add(entry);\n    }\n  }\n  for (const entry of parsed.entries) {\n    if (entry.status === \"active\" && expiredSet.has(entry.id) && !removedEntries.has(entry)) {\n      if (!expired.includes(entry.id)) expired.push(entry.id);\n      removedRanges.push([entry.blockStart, entry.blockEnd]);\n      removedEntries.add(entry);\n    }\n  }\n  const classesById = /* @__PURE__ */ new Map();\n  for (const finding of allFindings) {\n    if (!finding.id || !LOCAL_SPLICE_CLASSES.has(finding.cls) || expiredSet.has(finding.id)) continue;\n    const classes = classesById.get(finding.id) ?? /* @__PURE__ */ new Set();\n    classes.add(finding.cls);\n    classesById.set(finding.id, classes);\n  }\n  for (const entry of parsed.entries) {\n    if (expiredSet.has(entry.id) || !classesById.has(entry.id)) continue;\n    if (!removedEntries.has(entry)) {\n      removedRanges.push([entry.blockStart, entry.blockEnd]);\n      removedEntries.add(entry);\n    }\n  }\n  const spliced = [...classesById.entries()].filter(([id]) => parsed.entries.some((entry) => entry.id === id && !expiredSet.has(entry.id))).map(([id, classes]) => ({ id, classes: [...classes].sort() })).sort((a, b) => a.id === b.id ? 0 : a.id < b.id ? -1 : 1);\n  const invalidHandoff = allFindings.some((finding) => finding.cls === \"invalid-handoff\");\n  if (invalidHandoff && parsed.handoffRange) removedRanges.push(parsed.handoffRange);\n  let candidate = spliceRanges(parsed.normalized, removedRanges);\n  const protectedIds = invalidHandoff ? /* @__PURE__ */ new Set() : new Set(parsed.handoffReadFirst ?? []);\n  const remaining = parsed.entries.filter((entry) => entry.status === \"active\" && !removedEntries.has(entry));\n  remaining.sort((a, b) => {\n    const aDate = a.idDate ?? \"\";\n    const bDate = b.idDate ?? \"\";\n    if (aDate !== bDate) return aDate < bDate ? -1 : 1;\n    return a.id === b.id ? 0 : a.id < b.id ? -1 : 1;\n  });\n  const shedOrder = [...remaining.filter((entry) => !protectedIds.has(entry.id)), ...remaining.filter((entry) => protectedIds.has(entry.id))];\n  const global = allFindings.find((finding) => GLOBAL_BLANK_CLASSES.has(finding.cls));\n  for (const entry of global ? [] : shedOrder) {\n    if (!projectionBudget(frameMemoryProjection(candidate)).overBudget) break;\n    removedRanges.push([entry.blockStart, entry.blockEnd]);\n    removedEntries.add(entry);\n    if (!shed.includes(entry.id)) shed.push(entry.id);\n    candidate = spliceRanges(parsed.normalized, removedRanges);\n  }\n  let blanked = false;\n  if (global) blanked = { reason: global.cls };\n  else if (projectionBudget(frameMemoryProjection(candidate)).overBudget) blanked = { reason: \"budget-unrecoverable\" };\n  const framedCandidate = frameMemoryProjection(candidate);\n  const result = {\n    expired,\n    spliced,\n    shed,\n    blanked,\n    degraded: spliced.length > 0 || shed.length > 0,\n    injectText: blanked ? \"\" : framedCandidate,\n    budget: projectionBudget(framedCandidate)\n  };\n  Object.defineProperties(result, {\n    hard: { value: allFindings, enumerable: false },\n    dropped: { value: expired, enumerable: false },\n    conditionReview: { value: [], enumerable: false },\n    staleActive: { value: [], enumerable: false }\n  });\n  return result;\n}\n\n// src/main/memory-index/io.ts\nimport * as fs from \"fs\";\nimport * as path from \"path\";\nfunction hard2(cls, id, message, entry) {\n  return entry ? { cls, severity: \"hard\", id, message, blockStart: entry.blockStart, blockEnd: entry.blockEnd } : { cls, severity: \"hard\", id, message };\n}\nfunction isInsideDir(target, dir) {\n  const rel = path.relative(dir, target);\n  return rel !== \"\" && !rel.startsWith(\"..\") && !path.isAbsolute(rel);\n}\nfunction memoryDirs(workspaceRoot) {\n  const detailsDir = path.join(workspaceRoot, ...MEMORY_DETAILS_DIR.split(\"/\").filter(Boolean));\n  const archiveDir = path.join(workspaceRoot, ...MEMORY_ARCHIVE_DIR.split(\"/\").filter(Boolean));\n  const supervisorDir = path.resolve(detailsDir, \"..\", \"..\");\n  return {\n    supervisorDir,\n    detailsDir,\n    archiveDir,\n    memoryMd: path.join(supervisorDir, \"memory\", \"MEMORY.md\"),\n    archiveMd: path.join(workspaceRoot, ...ARCHIVE_INDEX_REL.split(\"/\").filter(Boolean))\n  };\n}\nfunction tryRealpath(p) {\n  try {\n    return fs.realpathSync.native(p);\n  } catch {\n    return null;\n  }\n}\nfunction readArchiveIfPresent(archiveMd) {\n  try {\n    return parseArchiveIndex(fs.readFileSync(archiveMd, \"utf8\"));\n  } catch (error) {\n    if (error.code === \"ENOENT\") return null;\n    throw error;\n  }\n}\nfunction epochDay(date) {\n  const ms = Date.parse(`${date}T00:00:00Z`);\n  return Number.isNaN(ms) ? NaN : Math.floor(ms / 864e5);\n}\nfunction validateIO(parsed, workspaceRoot, options = {}) {\n  const hardFindings = [];\n  const advisoryFindings = [];\n  const entryFindings = [];\n  const disposal = /* @__PURE__ */ new Map();\n  const expiredIds = [];\n  const conditionReview = [];\n  const { supervisorDir, detailsDir, archiveDir, archiveMd } = memoryDirs(workspaceRoot);\n  const canonicalDetailsDir = tryRealpath(detailsDir);\n  const canonicalArchiveDir = tryRealpath(archiveDir);\n  const archiveParsed = options.archiveParsed === void 0 ? readArchiveIfPresent(archiveMd) : options.archiveParsed;\n  const referencedDetails = /* @__PURE__ */ new Set();\n  const referencedArchive = /* @__PURE__ */ new Set();\n  const catalogs = [\n    { parsed, archived: false },\n    ...archiveParsed ? [{ parsed: archiveParsed, archived: true }] : []\n  ];\n  for (const catalog of catalogs) {\n    for (const entry of catalog.parsed.entries) {\n      const pointer = entry.detail;\n      if (!pointer) continue;\n      const candidate = path.resolve(supervisorDir, pointer);\n      const real = tryRealpath(candidate);\n      if (real === null) {\n        const finding = hard2(\"detail-missing\", entry.id, `detail file for ${entry.id} does not exist: ${pointer}`, entry);\n        hardFindings.push(finding);\n        if (!catalog.archived) entryFindings.push(finding);\n        if (!catalog.archived) disposal.set(entry.id, { error: \"detail-missing\" });\n        continue;\n      }\n      const inDetails = canonicalDetailsDir !== null && isInsideDir(real, canonicalDetailsDir);\n      const inArchive = canonicalArchiveDir !== null && isInsideDir(real, canonicalArchiveDir);\n      const expectedContained = catalog.archived ? inArchive : inDetails;\n      if (!expectedContained) {\n        const inKnownWrongRoot = catalog.archived ? inDetails : inArchive;\n        const cls = inKnownWrongRoot ? \"detail-root-mismatch\" : \"detail-escape\";\n        const expectedRoot = catalog.archived ? MEMORY_ARCHIVE_DIR : MEMORY_DETAILS_DIR;\n        const finding = hard2(cls, entry.id, `detail pointer for ${entry.id} does not resolve beneath ${expectedRoot}: ${pointer}`, entry);\n        hardFindings.push(finding);\n        if (!catalog.archived) entryFindings.push(finding);\n        if (!catalog.archived) disposal.set(entry.id, { error: cls });\n        continue;\n      }\n      (catalog.archived ? referencedArchive : referencedDetails).add(real);\n      let body;\n      try {\n        body = fs.readFileSync(real, \"utf8\");\n      } catch (error) {\n        const message = error instanceof Error ? error.message : String(error);\n        const finding = hard2(\"detail-unreadable\", entry.id, `detail file for ${entry.id} could not be read: ${message}`, entry);\n        hardFindings.push(finding);\n        if (!catalog.archived) entryFindings.push(finding);\n        if (!catalog.archived) disposal.set(entry.id, { error: \"detail-unreadable\" });\n        continue;\n      }\n      if (catalog.archived) continue;\n      const parsedDisposal = parseDisposal(body);\n      if (!parsedDisposal.ok) {\n        const cls = parsedDisposal.error === \"missing\" ? \"disposal-missing\" : \"disposal-malformed\";\n        const finding = hard2(cls, entry.id, parsedDisposal.message, entry);\n        hardFindings.push(finding);\n        entryFindings.push(finding);\n        disposal.set(entry.id, { error: cls });\n        continue;\n      }\n      const fact = { kind: parsedDisposal.disposal.kind, value: parsedDisposal.disposal.value };\n      disposal.set(entry.id, fact);\n      if (fact.kind === \"expires\" && fact.value && options.nowISO) {\n        const expiry = epochDay(fact.value);\n        const today = epochDay(options.nowISO.slice(0, 10));\n        if (!Number.isNaN(expiry) && !Number.isNaN(today) && expiry < today) expiredIds.push(entry.id);\n      } else if (fact.kind === \"expires-when\") {\n        conditionReview.push(entry.id);\n      }\n    }\n  }\n  const scanOrphans = (dir, canonicalDir, referenced, cls) => {\n    if (canonicalDir === null) return;\n    let dirents = [];\n    try {\n      dirents = fs.readdirSync(dir, { withFileTypes: true });\n    } catch {\n      return;\n    }\n    for (const dirent of dirents) {\n      if (!dirent.isFile() && !dirent.isSymbolicLink()) continue;\n      if (cls === \"archive-orphan\" && dirent.name === \"ARCHIVE.md\") continue;\n      const real = tryRealpath(path.join(dir, dirent.name));\n      if (real !== null && !referenced.has(real)) {\n        hardFindings.push(hard2(cls, null, `${cls === \"archive-orphan\" ? \"archive\" : \"detail\"} body has no catalog entry: ${dirent.name}`));\n      }\n    }\n  };\n  scanOrphans(detailsDir, canonicalDetailsDir, referencedDetails, \"orphan-details\");\n  scanOrphans(archiveDir, canonicalArchiveDir, referencedArchive, \"archive-orphan\");\n  if (archiveParsed) {\n    const archiveValidation = validateArchiveParsed(archiveParsed);\n    hardFindings.push(...archiveValidation.hard);\n    advisoryFindings.push(...archiveValidation.advisory);\n  }\n  return {\n    hard: hardFindings,\n    advisory: advisoryFindings,\n    entryFindings,\n    nonProjectionFindings: hardFindings.filter((finding) => !entryFindings.includes(finding)),\n    disposal,\n    expiredIds: [...new Set(expiredIds)].sort(),\n    conditionReview: [...new Set(conditionReview)].sort(),\n    archiveParsed\n  };\n}\nfunction deriveWorkspaceRootFromIndexPath(indexPath) {\n  const dir = path.dirname(path.resolve(indexPath));\n  const suffix = path.join(\".lares\", \"supervisor\", \"memory\");\n  const tail = path.sep + suffix;\n  return dir.toLowerCase().endsWith(tail.toLowerCase()) ? dir.slice(0, dir.length - tail.length) : null;\n}\n\n// scripts/memory-index-cli-entry.ts\nvar EXIT_OK = 0;\nvar EXIT_HARD = 1;\nvar EXIT_RUNTIME = 2;\nfunction parseArgs(argv) {\n  const command = argv[0] ?? \"\";\n  let file = null;\n  let nowISO = null;\n  for (let i = 1; i < argv.length; i++) {\n    const a = argv[i];\n    if (a === \"--now\") {\n      nowISO = argv[++i] ?? null;\n    } else if (a.startsWith(\"--now=\")) {\n      nowISO = a.slice(\"--now=\".length);\n    } else if (!a.startsWith(\"-\")) {\n      file = a;\n    }\n  }\n  return { command, file, nowISO };\n}\nfunction readIndexFile(file) {\n  const resolved = path2.resolve(file);\n  const real = fs2.realpathSync(resolved);\n  return fs2.readFileSync(real, \"utf8\");\n}\nfunction printFindings(findings) {\n  for (const f of findings) {\n    const where = f.id ? ` [${f.id}]` : \"\";\n    process.stderr.write(`${f.severity.toUpperCase()} ${f.cls}${where}: ${f.message}\n`);\n  }\n}\nfunction main() {\n  const { command, file, nowISO } = parseArgs(process.argv.slice(2));\n  if (command !== \"validate\" && command !== \"project\") {\n    process.stderr.write(`usage: memory-index.mjs <validate|project> [--now <ISO>] <index-file>\n`);\n    return EXIT_RUNTIME;\n  }\n  if (!file) {\n    process.stderr.write(`error: no index file given\n`);\n    return EXIT_RUNTIME;\n  }\n  let text;\n  try {\n    text = readIndexFile(file);\n  } catch (err) {\n    process.stderr.write(`RUNTIME read error: ${err.message}\n`);\n    return EXIT_RUNTIME;\n  }\n  let parsed;\n  try {\n    parsed = parseIndex(text);\n  } catch (err) {\n    process.stderr.write(`RUNTIME parse error: ${err.message}\n`);\n    return EXIT_RUNTIME;\n  }\n  const pure = validateParsed(parsed);\n  const workspaceRoot = deriveWorkspaceRootFromIndexPath(file);\n  const ioHard = workspaceRoot ? validateIO(parsed, workspaceRoot).hard : [];\n  const hard3 = [...pure.hard, ...ioHard];\n  const advisory2 = pure.advisory;\n  if (command === \"validate\") {\n    process.stdout.write(JSON.stringify({ hard: hard3, advisory: advisory2 }, null, 2) + \"\\n\");\n    printFindings([...hard3, ...advisory2]);\n    return hard3.length > 0 ? EXIT_HARD : EXIT_OK;\n  }\n  const now = nowISO ?? (/* @__PURE__ */ new Date()).toISOString();\n  const projection = projectParsed(parsed, { nowISO: now });\n  if (hard3.length > 0) {\n    printFindings(hard3);\n    process.stderr.write(`refusing to project a HARD-invalid index\n`);\n    return EXIT_HARD;\n  }\n  process.stdout.write(projection.injectText);\n  process.stderr.write(\n    JSON.stringify({\n      expired: projection.expired,\n      spliced: projection.spliced,\n      shed: projection.shed,\n      blanked: projection.blanked,\n      degraded: projection.degraded,\n      budget: projection.budget,\n      advisory: advisory2\n    }) + \"\\n\"\n  );\n  return EXIT_OK;\n}\nprocess.exit(main());\n";
